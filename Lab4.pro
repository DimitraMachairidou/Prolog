%Dimitra Machairidou 4108
%-----------------------------------------------------------------------------------------

%-- ASKHSH 1

...(0,M,K,[]).
...(N,M,K,[M|B]) :- A is N-1,
					C is M+K,
					...(A,C,K,B).

%-----------------------------------------------------------------------------------------

%-- ASKHSH 2

common([],Y,[]).
common(X,[],[]).
common([A|B],[C|D],[E|F]) :- A\=C,!,common(B,D,F).
common([A|B],[C|D],[E|F]) :- A==C,E is 1,common(B,D,F).

%-----------------------------------------------------------------------------------------

%-- ASKHSH 3

freq([],[]).
freq(L,S) :- fail.

%-----------------------------------------------------------------------------------------

%-- ASKHSH 4

count([],0).
count([A],0).
count([A,B|D],C) :- A=<2*B,!,count(D,C).
count([A,B|D],C) :- B=<2*A,!,count(D,C).
count([A,B|D],C) :- A>2*B,E is C+1,count(D,E).
count([A,B|D],C) :- B>2*A,E is C+1,count(D,E).
